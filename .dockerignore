# Exclude the local vendor tree (the builder re-resolves deps from go.mod/go.sum
# so we don't ship a large context), test artifacts, DB files and binaries.
vendor/
*.db
*.db-wal
*.db-shm
*.test
webhookgw
.idea/
.vscode/
.DS_Store
